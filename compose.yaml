services:
  postgres:
    image: postgres:17
    environment:
      POSTGRES_DB: course
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d course"]
      interval: 2s
      timeout: 3s
      retries: 15

  api:
    build:
      context: .
      dockerfile: src/Api/Dockerfile
    environment:
      # Runtime: least-privilege login role (only EXECUTE + SELECT catalog)
      ConnectionStrings__Course: "Host=postgres;Port=5432;Database=course;Username=course_api_login;Password=api_secret_change_me"
      # Migration: elevated privileges for DbMigrator (CREATE tables, schemas)
      MIGRATION_CONNECTION_STRING: "Host=postgres;Port=5432;Database=course;Username=postgres;Password=postgres"
      COURSE_JWT_SIGNING_KEY: "${COURSE_JWT_SIGNING_KEY:-moduledev-week1-rotated-key-do-not-use-in-production-2026-aug}"
    depends_on:
      postgres:
        condition: service_healthy

  gateway:
    build:
      context: .
      dockerfile: src/Gateway/Dockerfile
    ports:
      - "8080:8080"
    environment:
      Api__BaseUrl: "http://api:8080"
    depends_on:
      - api

  cli:
    build:
      context: .
      dockerfile: src/Cli/Dockerfile
    environment:
      # CLI: separate login role with catalog write privileges
      ConnectionStrings__Course: "Host=postgres;Port=5432;Database=course;Username=course_cli_login;Password=cli_secret_change_me"
    # Keep cli container running so compose-build check passes.
    # docker compose run ignores this override and uses Dockerfile ENTRYPOINT.
    entrypoint: ["sleep", "infinity"]

volumes:
  pgdata: